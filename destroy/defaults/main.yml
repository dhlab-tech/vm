---
vm_destroy_name: "{{ omit }}"
vm_destroy_libvirt_images_path: "/var/lib/libvirt/images"
vm_destroy_libvirt_image_name: "{{ vm_destroy_name }}.img"
vm_destroy_config_iso: "{{ vm_destroy_name }}.iso"
vm_destroy_config_yml: "{{ vm_destroy_name }}.yml"
